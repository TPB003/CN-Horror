# 仓库协作规则

本文件适用于整个仓库。具体贡献流程见 [`CONTRIBUTING.md`](CONTRIBUTING.md)，分支、目录和发布边界见 [`docs/repository-management.md`](docs/repository-management.md)。玩家体验的验收清单以 [`docs/player-experience-audit.md`](docs/player-experience-audit.md) 为准；本文件不另设一套视觉审计标准。

## 内容与史料

- 新增或改写剧情中的历史、典籍、仪式和民俗事实时，优先查原典、可靠校勘本、博物馆/档案馆或学术研究。来源记录至少写明题名、版本或发布机构、卷次/篇目或具体章节、可访问链接，以及它支持的具体说法。
- 明确区分**典籍记载、地方实践、作品分析、创作转译**。原典没有记载的细节不得写成古已有之或全国通行的习俗；无法确认的内容标为待考，不补造引文、出处或地域结论。
- 每个剧情章节与核心超自然意象都要有来源登记。来源可以支持故事母题或历史背景，不代表虚构剧情本身是真实史事；笔仙、红白双煞等现代类型元素应说明与古代仪式/文本的差异。
- 引文保持必要长度，保留版本与上下文；现代作品只作分析和链接，不复制受版权保护的长段文本、图像、视频或音轨。

## 剧情与小说

- `story/` 中的 Ink 故事源是网站剧情与小说正文的唯一事实来源。稳定章节 ID、角色名、时间线、线索和结局条件；需要改变时同步检查所有引用章节。
- 小说是由故事源导出的发布物。不得只手工修改导出小说；剧情变更必须运行导出并提交同步结果。小说包含一条标明选择路径的连续主线，并完整呈现另一个结局。
- 任何有意偏离史料的怪谈设定均以创作转译标注，并在对应章节来源登记中说明改编关系。

## 应用、素材与玩家体验

- 前端按 React + TypeScript 组件组织；章节内容、UI、3D 场景、声音与存档逻辑保持清晰边界。新增章节遵循现有组件和内容数据接口，不把整章逻辑继续堆进单个入口文件。
- 12 个剧情界面各自占满视口，章节切换使用明确控件或键盘操作，不能依赖页面滚轮上下推进。桌面与手机均不得裁掉主要剧情、交互或声音控制。
- 所有交互可键盘访问且焦点清楚；弹层管理焦点并支持退出；遵循 `prefers-reduced-motion`。声音必须由用户操作后开始，提供持续可见的静音/音量控制；关键剧情线索须有视觉或文字替代，不能只靠听力发现。
- 生成图不得承担可核验文字、地图地名或史料引文。每项生成素材在素材登记中记录用途、生成工具/模型（可得时）、生成日期、提示词文件、参考素材、后期修改和权利状态；外部素材须记录原始来源、许可条款与署名要求。未核明授权的素材不得进入发布目录。
- 视觉、动效、移动端、键盘和音频验收遵守 `docs/player-experience-audit.md`。修改影响玩家体验时，PR 附桌面与手机截图/录屏及审计结果。

## 代码与验证

- 优先保持依赖少且可静态部署；新增运行时依赖需在 PR 说明用途、体积/维护影响和许可证。
- 改动完成后运行与改动匹配的类型检查、测试、剧情/来源校验及生产构建。提交前确认小说导出与故事源一致、生产资源可加载、控制台无新增错误。
- `dist/` 是 Vite 构建输出，不作为应用源码编辑；业务源码在 `src/`，对外静态资源在 `public/assets/`。不得手改构建产物来代替改源码。依赖目录、密钥、临时文件和个人机器配置不得提交。
- Cloudflare Pages 只托管构建后的前端，不在仓库写入账户凭据、API token 或私有环境值。部署项目、域名与账户级设置由有权访问该账户的项目维护者负责；发布流程按 `docs/repository-management.md` 执行。

## 协作原则

- 中文面向用户；代码与标识符使用英文。尊重既有用户改动，不覆盖、回滚或删除与当前任务无关的工作。
- 通过分支和 Pull Request 合入，不直接推送主分支。提交规则、审查内容与合并门槛以 `CONTRIBUTING.md` 为准。
